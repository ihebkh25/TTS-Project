services:
  tts-server:
    build: .
    ports:
      - "8085:8085"
    environment:
      - PORT=8085
      - RUST_LOG=info
    volumes:
      - ./models:/app/models:ro

  frontend:
    build: ./frontend
    ports:
      - "8082:80"
    depends_on:
      - tts-server
